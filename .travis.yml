# mystic-crypt .travis.yml

language: java

cache:
  directories:
  - $HOME/.m2

jdk:
    - openjdk11

notifications:
    email:
        recipients:
            - asterios.raptis@web.de
        on_failure: always
        
after_success:
    - bash <(curl -s https://codecov.io/bash)
