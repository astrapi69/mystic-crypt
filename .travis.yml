# mystic-crypt .travis.yml
install: gradle wrapper

language: java

cache:
  directories:
  - $HOME/.m2

jdk:
    - openjdk8

notifications:
    email:
        recipients:
            - asterios.raptis@web.de
        on_failure: always
        
after_success:
    - bash <(curl -s https://codecov.io/bash)
